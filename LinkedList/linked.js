class LinkedList {
  constructor(head = null) {
    this.head = head;
  }
  // append data
}

class Node {
  constructor(data) {
    this.data = data;
    this.next = null;
  }
}

let myList = new LinkedList();

console.log(myList);
